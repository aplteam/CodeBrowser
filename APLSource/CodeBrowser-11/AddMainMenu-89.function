 {refs}←AddMainMenu(sourceRefs parms collect);html
 refs←{⍬≡⍴⊃⍵:1 2⍴1⊃,⊃⍵ ⋄ ↑⊃⍵}parms GetSubNamespaces¨sourceRefs
 refs←({(⍵⍳⍵)=⍳⍴⍵}refs[;2])⌿refs        ⍝ Get rid of (additional) refs
 html←'<nav id="main">' '<p>Namespace structure:</p>'
 html,←1 AddMainMenu_ refs collect
 html,←⊂'</nav>'
 WriteHTML html
⍝Done
