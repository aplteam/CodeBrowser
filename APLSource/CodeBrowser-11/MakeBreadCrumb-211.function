 MakeBreadCrumb←{
     parts←'.'A.Split ⍵
     first←1⊃parts
     urls←{(⍳⍴⍵)↑¨⊂⍵}parts
     top←{'<a href="#',(⊃{⍺,'.',⍵}/⍵),'">'}¨urls
     r←top,¨parts
     first,{((~0∊⍴⍵)/'.'),⍵}¯1↓⊃,/(1↓r),¨⊂'</a>.'
 }
