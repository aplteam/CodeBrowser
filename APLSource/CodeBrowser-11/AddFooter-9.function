 {r}←AddFooter parms;html
 r←⍬
 html←''
 :If ~0∊⍴parms.(author,date)
     html←⊂'<footer>'
     html,←⊂'<hr>'
     :If (0∊⍴parms.date)∧~0∊⍴parms.author
         html,←⊂'<p>Created by: ',parms.author,'</p>'
     :ElseIf (0∊⍴parms.author)∧~0∊⍴parms.date
         html,←⊂'<p>Created at: ',(FormatDate parms.date),'</p>'
     :Else
         html,←⊂'<p>Created by ',parms.author,' &mdash; Created at ',(FormatDate parms.date),'</p>'
     :EndIf
     html,←⊂'</footer>'
     WriteHTML html
 :EndIf
⍝Done
